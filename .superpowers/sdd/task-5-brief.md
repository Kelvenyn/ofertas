# Task 5: Privacy Policy Page

**Files:**
- Create: `src/app/psicopedagogia/politica-de-privacidade/page.tsx`

**Goal:** Create the privacy policy page for psicopedagogia, adapted from lembrancinhas version.

**Context:** Creates directory `src/app/psicopedagogia/politica-de-privacidade/` and writes the page file. Same structure as lembrancinhas version but with psicopedagogia-specific backlink, colors, and references.

**Steps:**
1. Create directory `src/app/psicopedagogia/politica-de-privacidade/` if needed
2. Create `page.tsx` with the complete code
3. Run `npx tsc --noEmit src/app/psicopedagogia/politica-de-privacidade/page.tsx` to verify

The code is (adapt from lembrancinhas version): backlink to `/psicopedagogia`, brand color `#0D4F4F`, brand-ink `#062B2B`, bg `#F5F8F0`.
